// const getFullName = (name: string, surname: string): string => {
// 	return name + ' ' + surname;
// };

// console.log(getFullName('Sam', 'Bencivengo'));
interface User {
	name: string;
	age?: number;
}

const user: User = {
	name: 'Sam',
	age: 30,
};

const user2: User = {
	name: 'Sam',
	age: 30,
};


